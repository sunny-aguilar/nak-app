import 'dart:io';
import 'dart:convert';

import 'package:flutter/services.dart';

class Chapters {
  // final String name;
  // final String epoch;
  // final String contact;

  // Chapters(this.name, this.epoch, this.contact);

  // Chapters.fromJson(Map<String, dynamic> json)
  //     : name = json["name"],
  //       epoch = json["epoch"],
  //       contact = json["contact"];

  // Map<String, dynamic> toJson() => {
  //       "name": name,
  //       "epoch": epoch,
  //       "contact": contact,
  //     };
}
